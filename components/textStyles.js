import { StyleSheet } from "react-native";

const textStyles = StyleSheet.create({
    fontSmall: {
        fontSize: 15
    },
    fontMedium: {
        fontSize: 18
    },

    fontLarge: {
        fontSize: 26
    },

    bold: {
        fontWeight: 500
    }
})

export default textStyles;